---
import { Image } from "astro:assets";
import energyArenaImage from "@images/energy-arena.jpg";
---

<section class="mv">
  <p class="catch">呪力ゼロ、拳のみ、勝者あり</p>
  <h1 class="title">エネルギー・・・吸収・・・アリーナ</h1>

  <figure class="thumb">
    <Image
      src={energyArenaImage}
      alt="呪力ゼロ、拳のみ、勝者あり。エネルギー・・・吸収・・・アリーナ"
    />
  </figure>
</section>

<style lang="scss">
  .catch {
    font-size: 2.5rem;
    text-align: center;
    animation: focus-in-expand 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }

  .title {
    margin-top: 30px;
    font-size: 4rem;
    text-align: center;
    animation: focus-in-expand 1.2s 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94)
      both;
  }

  .thumb {
    width: 50%;
    margin: 30px auto 0;
    animation: puff-in-center 1s 2.5s cubic-bezier(0.47, 0, 0.745, 0.715) both;
  }

  // TODO: アニメーションのコードを移動する
  @keyframes focus-in-expand {
    0% {
      letter-spacing: -0.5em;
      filter: blur(12px);
      opacity: 0;
    }

    100% {
      filter: blur(0);
      opacity: 1;
    }
  }

  @keyframes puff-in-center {
    0% {
      transform: scale(1.4);
      filter: blur(4px);
      opacity: 0;
    }

    100% {
      transform: scale(1);
      filter: blur(0);
      opacity: 1;
    }
  }
</style>
